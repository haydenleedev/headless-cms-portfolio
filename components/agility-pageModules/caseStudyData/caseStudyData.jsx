const CaseStudyData = () => {
  // this module doesn't render anything: it only provides additional data for LatestCustomerStories and CustomerStoryCards.
  return null;
};

export default CaseStudyData;
