const Placeholder = ({ module }) => {
  const { fields } = module;
  return (
    <section className="section padding-unset">
      <div className="container"></div>
    </section>
  );
};

export default Placeholder;
