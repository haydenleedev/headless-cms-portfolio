// this is an utility module which hides the navbar completely if this module is placed first on the page module list in agility.

const HideNavbar = () => {
  return <section data-navbar-hidden="true"></section>;
};

export default HideNavbar;
